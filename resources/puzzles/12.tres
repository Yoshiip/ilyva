[gd_resource type="Resource" load_steps=3 format=2]

[ext_resource path="res://scripts/resources/Puzzle.gd" type="Script" id=1]
[ext_resource path="res://images/portraits/st_philibert/mathieu.png" type="Texture" id=2]

[resource]
script = ExtResource( 1 )
portrait = ExtResource( 2 )
name = "Libérer Mathieu!"
start_phrase = "Libérer Mathieu!"
description = "C’est l’heure de libérer M. Mathieu! "
given_by = "Everaere"
hints = [ "Il vous faut savoir quel est le numéro de cellule de Mathieu et ensuite regarder le code qui correspond dans le fichier .codes.csv.", "Pour faire cela en une seule commande, lisez ce fichier avec la commande cat, limitez à une seule ligne en fonction du numéro de cellule avec la commande grep et coupez (avec la commande cut) en fonction de la virgule, et la réponse est le champ numéro 2." ]
hints_action = PoolStringArray( "", "", "" )
additional_apps = PoolStringArray(  )
